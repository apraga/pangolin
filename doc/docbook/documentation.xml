<chapter>
  <title>Documentation</title>
  <sect1 xml:id="doxygen_doc">
    <title>Source code</title>
    <para>
      The documentation generated by Doxygen is <ulink
        url="../../doxygen/pangolin_ref/index.html">available here</ulink>.
    </para>
  </sect1>
  <sect1>
    <title>Generating documentation</title>
    <sect2>
      <para> 
        Both doxygen and docbook documentation HTML are generated by going in
        the <filename>doc/</filename> folder and using the <command>make
          doc</command>. To generate only a part of the documentation, go in the
        corresponding folder and use <command>make doc</command> again. This
        will generate HTML documentation in
        <filename>docbook/user_manual</filename> for docbook and
        <filename>doxygen/pangolin_ref</filename> for Doxygen.
      </para>
      <para>
        To upload the documentation online, copy the files on the server. An
        example  is given in the <filename>Makefile</filename> for uploading the
        pages to a CERFACS personal webpage. 
      </para>
      <warning>
        When uploading the HTML documentation to a server, it is important to
        preserve the directory structure, otherwise relative link fails. This
        means you must upload <filename>docbook/user_manual</filename> and
        <filename>doxygen/pangolin_ref</filename> as they are.
      </warning>
      <para>
        A PDF version of the documentation can be generated by running
        <command>make pdf</command> in the appropriate directory
        (<filename>doc/docbook</filename> or <filename>doc/doxygen</filename>).
        It will generate a single pdf named
        <filename>pangolin_manual.pdf</filename> or
        <filename>pangolin_ref.pdf</filename>. To generate the pdf with doxygen,
        you must be able to use <command>pdflatex</command>. To customize this
        command, edit <filename>doxygen/Makefile</filename> as needed.
      </para>
    </sect2>

  </sect1>
</chapter>
